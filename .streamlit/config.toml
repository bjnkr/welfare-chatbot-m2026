[theme]
base = "light"
primaryColor = "#4A90E2"         # 버튼, 강조 색상 (세련된 파랑)
backgroundColor = "#FFFFFF"      # 전체 배경색 (완전한 흰색)
secondaryBackgroundColor = "#F8F9FA" # 사이드바, 입력창 배경색 (밝은 회색)
textColor = "#171717"            # 글자 색상 (진한 검정)
font = "sans serif"              # 깔끔한 고딕 폰트